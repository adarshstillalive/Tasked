import { useWebSocket } from "../context/webSocketContext";

const Home = () => {
  // const socket = useWebSocket();
  // socket.on("connect", () => {
  //   console.log("connected");
  // });
  return <div>Home</div>;
};

export default Home;
